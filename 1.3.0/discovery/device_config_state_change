message 5019 {
  optional INT32 activity_id (INTEGER(32, true));
  optional BYTE_ARRAY activity_name (STRING);
  optional group actor {
    optional BYTE_ARRAY app_name (STRING);
    optional BYTE_ARRAY app_uid (STRING);
    optional group authorizations (LIST) {
      repeated group list {
        optional BYTE_ARRAY decision (STRING);
        optional group policy {
          optional BYTE_ARRAY desc (STRING);
          optional group group {
            optional BYTE_ARRAY desc (STRING);
            optional BYTE_ARRAY domain (STRING);
            optional BYTE_ARRAY name (STRING);
            optional group privileges (LIST) {
              repeated group list {
              }
            }
            optional BYTE_ARRAY type (STRING);
            optional BYTE_ARRAY uid (STRING);
          }
          optional BOOLEAN is_applied;
          optional BYTE_ARRAY name (STRING);
          optional BYTE_ARRAY uid (STRING);
          optional BYTE_ARRAY version (STRING);
        }
      }
    }
    optional group idp {
      optional BYTE_ARRAY name (STRING);
      optional BYTE_ARRAY uid (STRING);
    }
    optional BYTE_ARRAY invoked_by (STRING);
    optional group process {
      optional BYTE_ARRAY cmd_line (STRING);
      optional INT64 created_time (INTEGER(64, true));
      optional group file {
        optional INT64 accessed_time (INTEGER(64, true));
        optional group accessor {
          optional group account {
            optional group labels (LIST) {
              repeated group list {
              }
            }
            optional BYTE_ARRAY name (STRING);
            optional BYTE_ARRAY type (STRING);
            optional INT32 type_id (INTEGER(32, true));
            optional BYTE_ARRAY uid (STRING);
          }
          optional BYTE_ARRAY credential_uid (STRING);
          optional BYTE_ARRAY domain (STRING);
          optional BYTE_ARRAY email_addr (STRING);
          optional BYTE_ARRAY full_name (STRING);
          optional group groups (LIST) {
            repeated group list {
              optional BYTE_ARRAY desc (STRING);
              optional BYTE_ARRAY domain (STRING);
              optional BYTE_ARRAY name (STRING);
              optional group privileges (LIST) {
                repeated group list {
                }
              }
              optional BYTE_ARRAY type (STRING);
              optional BYTE_ARRAY uid (STRING);
            }
          }
          optional BYTE_ARRAY ldap_person (JSON);
          optional BYTE_ARRAY name (STRING);
          optional group org {
            optional BYTE_ARRAY name (STRING);
            optional BYTE_ARRAY ou_name (STRING);
            optional BYTE_ARRAY ou_uid (STRING);
            optional BYTE_ARRAY uid (STRING);
          }
          optional BYTE_ARRAY risk_level (STRING);
          optional INT32 risk_level_id (INTEGER(32, true));
          optional INT64 risk_score (INTEGER(64, true));
          optional BYTE_ARRAY type (STRING);
          optional INT32 type_id (INTEGER(32, true));
          optional BYTE_ARRAY uid (STRING);
          optional BYTE_ARRAY uid_alt (STRING);
        }
        optional INT64 attributes (INTEGER(64, true));
        optional BYTE_ARRAY company_name (STRING);
        optional BYTE_ARRAY confidentiality (STRING);
        optional INT32 confidentiality_id (INTEGER(32, true));
        optional INT64 created_time (INTEGER(64, true));
        optional group creator {
          optional group account {
            optional group labels (LIST) {
              repeated group list {
              }
            }
            optional BYTE_ARRAY name (STRING);
            optional BYTE_ARRAY type (STRING);
            optional INT32 type_id (INTEGER(32, true));
            optional BYTE_ARRAY uid (STRING);
          }
          optional BYTE_ARRAY credential_uid (STRING);
          optional BYTE_ARRAY domain (STRING);
          optional BYTE_ARRAY email_addr (STRING);
          optional BYTE_ARRAY full_name (STRING);
          optional group groups (LIST) {
            repeated group list {
              optional BYTE_ARRAY desc (STRING);
              optional BYTE_ARRAY domain (STRING);
              optional BYTE_ARRAY name (STRING);
              optional group privileges (LIST) {
                repeated group list {
                }
              }
              optional BYTE_ARRAY type (STRING);
              optional BYTE_ARRAY uid (STRING);
            }
          }
          optional BYTE_ARRAY ldap_person (JSON);
          optional BYTE_ARRAY name (STRING);
          optional group org {
            optional BYTE_ARRAY name (STRING);
            optional BYTE_ARRAY ou_name (STRING);
            optional BYTE_ARRAY ou_uid (STRING);
            optional BYTE_ARRAY uid (STRING);
          }
          optional BYTE_ARRAY risk_level (STRING);
          optional INT32 risk_level_id (INTEGER(32, true));
          optional INT64 risk_score (INTEGER(64, true));
          optional BYTE_ARRAY type (STRING);
          optional INT32 type_id (INTEGER(32, true));
          optional BYTE_ARRAY uid (STRING);
          optional BYTE_ARRAY uid_alt (STRING);
        }
        optional BYTE_ARRAY desc (STRING);
        optional BYTE_ARRAY ext (STRING);
        optional group hashes (LIST) {
          repeated group list {
            optional BYTE_ARRAY algorithm (STRING);
            optional INT32 algorithm_id (INTEGER(32, true));
            optional BYTE_ARRAY value (STRING);
          }
        }
        optional BOOLEAN is_system;
        optional BYTE_ARRAY mime_type (STRING);
        optional INT64 modified_time (INTEGER(64, true));
        optional group modifier {
          optional group account {
            optional group labels (LIST) {
              repeated group list {
              }
            }
            optional BYTE_ARRAY name (STRING);
            optional BYTE_ARRAY type (STRING);
            optional INT32 type_id (INTEGER(32, true));
            optional BYTE_ARRAY uid (STRING);
          }
          optional BYTE_ARRAY credential_uid (STRING);
          optional BYTE_ARRAY domain (STRING);
          optional BYTE_ARRAY email_addr (STRING);
          optional BYTE_ARRAY full_name (STRING);
          optional group groups (LIST) {
            repeated group list {
              optional BYTE_ARRAY desc (STRING);
              optional BYTE_ARRAY domain (STRING);
              optional BYTE_ARRAY name (STRING);
              optional group privileges (LIST) {
                repeated group list {
                }
              }
              optional BYTE_ARRAY type (STRING);
              optional BYTE_ARRAY uid (STRING);
            }
          }
          optional BYTE_ARRAY ldap_person (JSON);
          optional BYTE_ARRAY name (STRING);
          optional group org {
            optional BYTE_ARRAY name (STRING);
            optional BYTE_ARRAY ou_name (STRING);
            optional BYTE_ARRAY ou_uid (STRING);
            optional BYTE_ARRAY uid (STRING);
          }
          optional BYTE_ARRAY risk_level (STRING);
          optional INT32 risk_level_id (INTEGER(32, true));
          optional INT64 risk_score (INTEGER(64, true));
          optional BYTE_ARRAY type (STRING);
          optional INT32 type_id (INTEGER(32, true));
          optional BYTE_ARRAY uid (STRING);
          optional BYTE_ARRAY uid_alt (STRING);
        }
        optional BYTE_ARRAY name (STRING);
        optional group owner {
          optional group account {
            optional group labels (LIST) {
              repeated group list {
              }
            }
            optional BYTE_ARRAY name (STRING);
            optional BYTE_ARRAY type (STRING);
            optional INT32 type_id (INTEGER(32, true));
            optional BYTE_ARRAY uid (STRING);
          }
          optional BYTE_ARRAY credential_uid (STRING);
          optional BYTE_ARRAY domain (STRING);
          optional BYTE_ARRAY email_addr (STRING);
          optional BYTE_ARRAY full_name (STRING);
          optional group groups (LIST) {
            repeated group list {
              optional BYTE_ARRAY desc (STRING);
              optional BYTE_ARRAY domain (STRING);
              optional BYTE_ARRAY name (STRING);
              optional group privileges (LIST) {
                repeated group list {
                }
              }
              optional BYTE_ARRAY type (STRING);
              optional BYTE_ARRAY uid (STRING);
            }
          }
          optional BYTE_ARRAY ldap_person (JSON);
          optional BYTE_ARRAY name (STRING);
          optional group org {
            optional BYTE_ARRAY name (STRING);
            optional BYTE_ARRAY ou_name (STRING);
            optional BYTE_ARRAY ou_uid (STRING);
            optional BYTE_ARRAY uid (STRING);
          }
          optional BYTE_ARRAY risk_level (STRING);
          optional INT32 risk_level_id (INTEGER(32, true));
          optional INT64 risk_score (INTEGER(64, true));
          optional BYTE_ARRAY type (STRING);
          optional INT32 type_id (INTEGER(32, true));
          optional BYTE_ARRAY uid (STRING);
          optional BYTE_ARRAY uid_alt (STRING);
        }
        optional BYTE_ARRAY parent_folder (STRING);
        optional BYTE_ARRAY path (STRING);
        optional group product {
          optional BYTE_ARRAY cpe_name (STRING);
          optional group feature {
            optional BYTE_ARRAY name (STRING);
            optional BYTE_ARRAY uid (STRING);
            optional BYTE_ARRAY version (STRING);
          }
          optional BYTE_ARRAY lang (STRING);
          optional BYTE_ARRAY name (STRING);
          optional BYTE_ARRAY path (STRING);
          optional BYTE_ARRAY uid (STRING);
          optional BYTE_ARRAY url_string (STRING);
          optional BYTE_ARRAY vendor_name (STRING);
          optional BYTE_ARRAY version (STRING);
        }
        optional BYTE_ARRAY security_descriptor (STRING);
        optional group signature {
          optional BYTE_ARRAY algorithm (STRING);
          optional INT32 algorithm_id (INTEGER(32, true));
          optional group certificate {
            optional INT64 created_time (INTEGER(64, true));
            optional INT64 expiration_time (INTEGER(64, true));
            optional group fingerprints (LIST) {
              repeated group list {
                optional BYTE_ARRAY algorithm (STRING);
                optional INT32 algorithm_id (INTEGER(32, true));
                optional BYTE_ARRAY value (STRING);
              }
            }
            optional BOOLEAN is_self_signed;
            optional BYTE_ARRAY issuer (STRING);
            optional BYTE_ARRAY serial_number (STRING);
            optional BYTE_ARRAY subject (STRING);
            optional BYTE_ARRAY uid (STRING);
            optional BYTE_ARRAY version (STRING);
          }
          optional INT64 created_time (INTEGER(64, true));
          optional BYTE_ARRAY developer_uid (STRING);
          optional group digest {
            optional BYTE_ARRAY algorithm (STRING);
            optional INT32 algorithm_id (INTEGER(32, true));
            optional BYTE_ARRAY value (STRING);
          }
          optional BYTE_ARRAY state (STRING);
          optional INT32 state_id (INTEGER(32, true));
        }
        optional INT64 size (INTEGER(64, true));
        optional BYTE_ARRAY type (STRING);
        optional INT32 type_id (INTEGER(32, true));
        optional BYTE_ARRAY uid (STRING);
        optional BYTE_ARRAY version (STRING);
        optional BYTE_ARRAY xattributes (JSON);
      }
      optional BYTE_ARRAY integrity (STRING);
      optional INT32 integrity_id (INTEGER(32, true));
      optional group lineage (LIST) {
        repeated group list {
        }
      }
      optional group loaded_modules (LIST) {
        repeated group list {
        }
      }
      optional BYTE_ARRAY name (STRING);
      optional BYTE_ARRAY parent_process (JSON);
      optional INT64 pid (INTEGER(64, true));
      optional BYTE_ARRAY sandbox (STRING);
      optional group session {
        optional INT64 count (INTEGER(64, true));
        optional INT64 created_time (INTEGER(64, true));
        optional BYTE_ARRAY credential_uid (STRING);
        optional BYTE_ARRAY expiration_reason (STRING);
        optional INT64 expiration_time (INTEGER(64, true));
        optional BOOLEAN is_mfa;
        optional BOOLEAN is_remote;
        optional BOOLEAN is_vpn;
        optional BYTE_ARRAY issuer (STRING);
        optional BYTE_ARRAY terminal (STRING);
        optional BYTE_ARRAY uid (STRING);
        optional BYTE_ARRAY uid_alt (STRING);
        optional BYTE_ARRAY uuid (STRING);
      }
      optional INT64 terminated_time (INTEGER(64, true));
      optional INT64 tid (INTEGER(64, true));
      optional BYTE_ARRAY uid (STRING);
      optional group user {
        optional group account {
          optional group labels (LIST) {
            repeated group list {
            }
          }
          optional BYTE_ARRAY name (STRING);
          optional BYTE_ARRAY type (STRING);
          optional INT32 type_id (INTEGER(32, true));
          optional BYTE_ARRAY uid (STRING);
        }
        optional BYTE_ARRAY credential_uid (STRING);
        optional BYTE_ARRAY domain (STRING);
        optional BYTE_ARRAY email_addr (STRING);
        optional BYTE_ARRAY full_name (STRING);
        optional group groups (LIST) {
          repeated group list {
            optional BYTE_ARRAY desc (STRING);
            optional BYTE_ARRAY domain (STRING);
            optional BYTE_ARRAY name (STRING);
            optional group privileges (LIST) {
              repeated group list {
              }
            }
            optional BYTE_ARRAY type (STRING);
            optional BYTE_ARRAY uid (STRING);
          }
        }
        optional BYTE_ARRAY ldap_person (JSON);
        optional BYTE_ARRAY name (STRING);
        optional group org {
          optional BYTE_ARRAY name (STRING);
          optional BYTE_ARRAY ou_name (STRING);
          optional BYTE_ARRAY ou_uid (STRING);
          optional BYTE_ARRAY uid (STRING);
        }
        optional BYTE_ARRAY risk_level (STRING);
        optional INT32 risk_level_id (INTEGER(32, true));
        optional INT64 risk_score (INTEGER(64, true));
        optional BYTE_ARRAY type (STRING);
        optional INT32 type_id (INTEGER(32, true));
        optional BYTE_ARRAY uid (STRING);
        optional BYTE_ARRAY uid_alt (STRING);
      }
      optional BYTE_ARRAY xattributes (JSON);
    }
    optional group session {
      optional INT64 count (INTEGER(64, true));
      optional INT64 created_time (INTEGER(64, true));
      optional BYTE_ARRAY credential_uid (STRING);
      optional BYTE_ARRAY expiration_reason (STRING);
      optional INT64 expiration_time (INTEGER(64, true));
      optional BOOLEAN is_mfa;
      optional BOOLEAN is_remote;
      optional BOOLEAN is_vpn;
      optional BYTE_ARRAY issuer (STRING);
      optional BYTE_ARRAY terminal (STRING);
      optional BYTE_ARRAY uid (STRING);
      optional BYTE_ARRAY uid_alt (STRING);
      optional BYTE_ARRAY uuid (STRING);
    }
    optional group user {
      optional group account {
        optional group labels (LIST) {
          repeated group list {
          }
        }
        optional BYTE_ARRAY name (STRING);
        optional BYTE_ARRAY type (STRING);
        optional INT32 type_id (INTEGER(32, true));
        optional BYTE_ARRAY uid (STRING);
      }
      optional BYTE_ARRAY credential_uid (STRING);
      optional BYTE_ARRAY domain (STRING);
      optional BYTE_ARRAY email_addr (STRING);
      optional BYTE_ARRAY full_name (STRING);
      optional group groups (LIST) {
        repeated group list {
          optional BYTE_ARRAY desc (STRING);
          optional BYTE_ARRAY domain (STRING);
          optional BYTE_ARRAY name (STRING);
          optional group privileges (LIST) {
            repeated group list {
            }
          }
          optional BYTE_ARRAY type (STRING);
          optional BYTE_ARRAY uid (STRING);
        }
      }
      optional BYTE_ARRAY ldap_person (JSON);
      optional BYTE_ARRAY name (STRING);
      optional group org {
        optional BYTE_ARRAY name (STRING);
        optional BYTE_ARRAY ou_name (STRING);
        optional BYTE_ARRAY ou_uid (STRING);
        optional BYTE_ARRAY uid (STRING);
      }
      optional BYTE_ARRAY risk_level (STRING);
      optional INT32 risk_level_id (INTEGER(32, true));
      optional INT64 risk_score (INTEGER(64, true));
      optional BYTE_ARRAY type (STRING);
      optional INT32 type_id (INTEGER(32, true));
      optional BYTE_ARRAY uid (STRING);
      optional BYTE_ARRAY uid_alt (STRING);
    }
  }
  optional BYTE_ARRAY category_name (STRING);
  optional INT64 category_uid (INTEGER(64, true));
  optional BYTE_ARRAY class_name (STRING);
  optional INT64 class_uid (INTEGER(64, true));
  optional INT64 count (INTEGER(64, true));
  optional group device {
    optional group agent_list (LIST) {
      repeated group list {
        optional BYTE_ARRAY name (STRING);
        optional group policies (LIST) {
          repeated group list {
            optional BYTE_ARRAY desc (STRING);
            optional group group {
              optional BYTE_ARRAY desc (STRING);
              optional BYTE_ARRAY domain (STRING);
              optional BYTE_ARRAY name (STRING);
              optional group privileges (LIST) {
                repeated group list {
                }
              }
              optional BYTE_ARRAY type (STRING);
              optional BYTE_ARRAY uid (STRING);
            }
            optional BOOLEAN is_applied;
            optional BYTE_ARRAY name (STRING);
            optional BYTE_ARRAY uid (STRING);
            optional BYTE_ARRAY version (STRING);
          }
        }
        optional BYTE_ARRAY type (STRING);
        optional INT32 type_id (INTEGER(32, true));
        optional BYTE_ARRAY uid (STRING);
        optional BYTE_ARRAY uid_alt (STRING);
        optional BYTE_ARRAY vendor_name (STRING);
        optional BYTE_ARRAY version (STRING);
      }
    }
    optional BYTE_ARRAY autoscale_uid (STRING);
    optional INT64 boot_time (INTEGER(64, true));
    optional INT64 created_time (INTEGER(64, true));
    optional BYTE_ARRAY desc (STRING);
    optional BYTE_ARRAY domain (STRING);
    optional INT64 first_seen_time (INTEGER(64, true));
    optional group groups (LIST) {
      repeated group list {
        optional BYTE_ARRAY desc (STRING);
        optional BYTE_ARRAY domain (STRING);
        optional BYTE_ARRAY name (STRING);
        optional group privileges (LIST) {
          repeated group list {
          }
        }
        optional BYTE_ARRAY type (STRING);
        optional BYTE_ARRAY uid (STRING);
      }
    }
    optional BYTE_ARRAY hostname (STRING);
    optional group hw_info {
      optional BYTE_ARRAY bios_date (STRING);
      optional BYTE_ARRAY bios_manufacturer (STRING);
      optional BYTE_ARRAY bios_ver (STRING);
      optional BYTE_ARRAY chassis (STRING);
      optional INT64 cpu_bits (INTEGER(64, true));
      optional INT64 cpu_cores (INTEGER(64, true));
      optional INT64 cpu_count (INTEGER(64, true));
      optional INT64 cpu_speed (INTEGER(64, true));
      optional BYTE_ARRAY cpu_type (STRING);
      optional group desktop_display {
        optional INT64 color_depth (INTEGER(64, true));
        optional INT64 physical_height (INTEGER(64, true));
        optional INT64 physical_orientation (INTEGER(64, true));
        optional INT64 physical_width (INTEGER(64, true));
        optional INT64 scale_factor (INTEGER(64, true));
      }
      optional group keyboard_info {
        optional INT64 function_keys (INTEGER(64, true));
        optional BYTE_ARRAY ime (STRING);
        optional BYTE_ARRAY keyboard_layout (STRING);
        optional INT64 keyboard_subtype (INTEGER(64, true));
        optional BYTE_ARRAY keyboard_type (STRING);
      }
      optional INT64 ram_size (INTEGER(64, true));
      optional BYTE_ARRAY serial_number (STRING);
    }
    optional BYTE_ARRAY hypervisor (STRING);
    optional group image {
      optional group labels (LIST) {
        repeated group list {
        }
      }
      optional BYTE_ARRAY name (STRING);
      optional BYTE_ARRAY path (STRING);
      optional BYTE_ARRAY tag (STRING);
      optional BYTE_ARRAY uid (STRING);
    }
    optional BYTE_ARRAY imei (STRING);
    optional BYTE_ARRAY instance_uid (STRING);
    optional BYTE_ARRAY interface_name (STRING);
    optional BYTE_ARRAY interface_uid (STRING);
    optional BYTE_ARRAY ip (STRING);
    optional BOOLEAN is_compliant;
    optional BOOLEAN is_managed;
    optional BOOLEAN is_personal;
    optional BOOLEAN is_trusted;
    optional INT64 last_seen_time (INTEGER(64, true));
    optional group location {
      optional BYTE_ARRAY city (STRING);
      optional BYTE_ARRAY continent (STRING);
      optional group coordinates (LIST) {
        repeated group list {
        }
      }
      optional BYTE_ARRAY country (STRING);
      optional BYTE_ARRAY desc (STRING);
      optional BYTE_ARRAY geohash (STRING);
      optional BOOLEAN is_on_premises;
      optional BYTE_ARRAY isp (STRING);
      optional DOUBLE lat;
      optional DOUBLE long;
      optional BYTE_ARRAY postal_code (STRING);
      optional BYTE_ARRAY provider (STRING);
      optional BYTE_ARRAY region (STRING);
    }
    optional BYTE_ARRAY mac (STRING);
    optional INT64 modified_time (INTEGER(64, true));
    optional BYTE_ARRAY name (STRING);
    optional group network_interfaces (LIST) {
      repeated group list {
        optional BYTE_ARRAY hostname (STRING);
        optional BYTE_ARRAY ip (STRING);
        optional BYTE_ARRAY mac (STRING);
        optional BYTE_ARRAY name (STRING);
        optional BYTE_ARRAY namespace (STRING);
        optional INT64 subnet_prefix (INTEGER(64, true));
        optional BYTE_ARRAY type (STRING);
        optional INT32 type_id (INTEGER(32, true));
        optional BYTE_ARRAY uid (STRING);
      }
    }
    optional group org {
      optional BYTE_ARRAY name (STRING);
      optional BYTE_ARRAY ou_name (STRING);
      optional BYTE_ARRAY ou_uid (STRING);
      optional BYTE_ARRAY uid (STRING);
    }
    optional group os {
      optional BYTE_ARRAY build (STRING);
      optional BYTE_ARRAY country (STRING);
      optional BYTE_ARRAY cpe_name (STRING);
      optional INT64 cpu_bits (INTEGER(64, true));
      optional BYTE_ARRAY edition (STRING);
      optional BYTE_ARRAY lang (STRING);
      optional BYTE_ARRAY name (STRING);
      optional BYTE_ARRAY sp_name (STRING);
      optional INT64 sp_ver (INTEGER(64, true));
      optional BYTE_ARRAY type (STRING);
      optional INT32 type_id (INTEGER(32, true));
      optional BYTE_ARRAY version (STRING);
    }
    optional group owner {
      optional group account {
        optional group labels (LIST) {
          repeated group list {
          }
        }
        optional BYTE_ARRAY name (STRING);
        optional BYTE_ARRAY type (STRING);
        optional INT32 type_id (INTEGER(32, true));
        optional BYTE_ARRAY uid (STRING);
      }
      optional BYTE_ARRAY credential_uid (STRING);
      optional BYTE_ARRAY domain (STRING);
      optional BYTE_ARRAY email_addr (STRING);
      optional BYTE_ARRAY full_name (STRING);
      optional group groups (LIST) {
        repeated group list {
          optional BYTE_ARRAY desc (STRING);
          optional BYTE_ARRAY domain (STRING);
          optional BYTE_ARRAY name (STRING);
          optional group privileges (LIST) {
            repeated group list {
            }
          }
          optional BYTE_ARRAY type (STRING);
          optional BYTE_ARRAY uid (STRING);
        }
      }
      optional BYTE_ARRAY ldap_person (JSON);
      optional BYTE_ARRAY name (STRING);
      optional group org {
        optional BYTE_ARRAY name (STRING);
        optional BYTE_ARRAY ou_name (STRING);
        optional BYTE_ARRAY ou_uid (STRING);
        optional BYTE_ARRAY uid (STRING);
      }
      optional BYTE_ARRAY risk_level (STRING);
      optional INT32 risk_level_id (INTEGER(32, true));
      optional INT64 risk_score (INTEGER(64, true));
      optional BYTE_ARRAY type (STRING);
      optional INT32 type_id (INTEGER(32, true));
      optional BYTE_ARRAY uid (STRING);
      optional BYTE_ARRAY uid_alt (STRING);
    }
    optional BYTE_ARRAY region (STRING);
    optional BYTE_ARRAY risk_level (STRING);
    optional INT32 risk_level_id (INTEGER(32, true));
    optional INT64 risk_score (INTEGER(64, true));
    optional BYTE_ARRAY subnet (STRING);
    optional BYTE_ARRAY subnet_uid (STRING);
    optional BYTE_ARRAY type (STRING);
    optional INT32 type_id (INTEGER(32, true));
    optional BYTE_ARRAY uid (STRING);
    optional BYTE_ARRAY uid_alt (STRING);
    optional BYTE_ARRAY vlan_uid (STRING);
    optional BYTE_ARRAY vpc_uid (STRING);
    optional BYTE_ARRAY zone (STRING);
  }
  optional INT64 duration (INTEGER(64, true));
  optional INT64 end_time (INTEGER(64, true));
  optional group enrichments (LIST) {
    repeated group list {
      optional INT64 created_time (INTEGER(64, true));
      optional BYTE_ARRAY data (JSON);
      optional BYTE_ARRAY desc (STRING);
      optional BYTE_ARRAY name (STRING);
      optional BYTE_ARRAY provider (STRING);
      optional group reputation {
        optional DOUBLE base_score;
        optional BYTE_ARRAY provider (STRING);
        optional BYTE_ARRAY score (STRING);
        optional INT32 score_id (INTEGER(32, true));
      }
      optional BYTE_ARRAY short_desc (STRING);
      optional BYTE_ARRAY src_url (STRING);
      optional BYTE_ARRAY type (STRING);
      optional BYTE_ARRAY value (STRING);
    }
  }
  optional BYTE_ARRAY message (STRING);
  optional group metadata {
    optional BYTE_ARRAY correlation_uid (STRING);
    optional BYTE_ARRAY event_code (STRING);
    optional group extension {
      optional BYTE_ARRAY name (STRING);
      optional BYTE_ARRAY uid (STRING);
      optional BYTE_ARRAY version (STRING);
    }
    optional group extensions (LIST) {
      repeated group list {
        optional BYTE_ARRAY name (STRING);
        optional BYTE_ARRAY uid (STRING);
        optional BYTE_ARRAY version (STRING);
      }
    }
    optional group labels (LIST) {
      repeated group list {
      }
    }
    optional BYTE_ARRAY log_level (STRING);
    optional BYTE_ARRAY log_name (STRING);
    optional BYTE_ARRAY log_provider (STRING);
    optional BYTE_ARRAY log_version (STRING);
    optional INT64 logged_time (INTEGER(64, true));
    optional group loggers (LIST) {
      repeated group list {
        optional group device {
          optional group agent_list (LIST) {
            repeated group list {
              optional BYTE_ARRAY name (STRING);
              optional group policies (LIST) {
                repeated group list {
                  optional BYTE_ARRAY desc (STRING);
                  optional group group {
                    optional BYTE_ARRAY desc (STRING);
                    optional BYTE_ARRAY domain (STRING);
                    optional BYTE_ARRAY name (STRING);
                    optional group privileges (LIST) {
                      repeated group list {
                      }
                    }
                    optional BYTE_ARRAY type (STRING);
                    optional BYTE_ARRAY uid (STRING);
                  }
                  optional BOOLEAN is_applied;
                  optional BYTE_ARRAY name (STRING);
                  optional BYTE_ARRAY uid (STRING);
                  optional BYTE_ARRAY version (STRING);
                }
              }
              optional BYTE_ARRAY type (STRING);
              optional INT32 type_id (INTEGER(32, true));
              optional BYTE_ARRAY uid (STRING);
              optional BYTE_ARRAY uid_alt (STRING);
              optional BYTE_ARRAY vendor_name (STRING);
              optional BYTE_ARRAY version (STRING);
            }
          }
          optional BYTE_ARRAY autoscale_uid (STRING);
          optional INT64 boot_time (INTEGER(64, true));
          optional INT64 created_time (INTEGER(64, true));
          optional BYTE_ARRAY desc (STRING);
          optional BYTE_ARRAY domain (STRING);
          optional INT64 first_seen_time (INTEGER(64, true));
          optional group groups (LIST) {
            repeated group list {
              optional BYTE_ARRAY desc (STRING);
              optional BYTE_ARRAY domain (STRING);
              optional BYTE_ARRAY name (STRING);
              optional group privileges (LIST) {
                repeated group list {
                }
              }
              optional BYTE_ARRAY type (STRING);
              optional BYTE_ARRAY uid (STRING);
            }
          }
          optional BYTE_ARRAY hostname (STRING);
          optional group hw_info {
            optional BYTE_ARRAY bios_date (STRING);
            optional BYTE_ARRAY bios_manufacturer (STRING);
            optional BYTE_ARRAY bios_ver (STRING);
            optional BYTE_ARRAY chassis (STRING);
            optional INT64 cpu_bits (INTEGER(64, true));
            optional INT64 cpu_cores (INTEGER(64, true));
            optional INT64 cpu_count (INTEGER(64, true));
            optional INT64 cpu_speed (INTEGER(64, true));
            optional BYTE_ARRAY cpu_type (STRING);
            optional group desktop_display {
              optional INT64 color_depth (INTEGER(64, true));
              optional INT64 physical_height (INTEGER(64, true));
              optional INT64 physical_orientation (INTEGER(64, true));
              optional INT64 physical_width (INTEGER(64, true));
              optional INT64 scale_factor (INTEGER(64, true));
            }
            optional group keyboard_info {
              optional INT64 function_keys (INTEGER(64, true));
              optional BYTE_ARRAY ime (STRING);
              optional BYTE_ARRAY keyboard_layout (STRING);
              optional INT64 keyboard_subtype (INTEGER(64, true));
              optional BYTE_ARRAY keyboard_type (STRING);
            }
            optional INT64 ram_size (INTEGER(64, true));
            optional BYTE_ARRAY serial_number (STRING);
          }
          optional BYTE_ARRAY hypervisor (STRING);
          optional group image {
            optional group labels (LIST) {
              repeated group list {
              }
            }
            optional BYTE_ARRAY name (STRING);
            optional BYTE_ARRAY path (STRING);
            optional BYTE_ARRAY tag (STRING);
            optional BYTE_ARRAY uid (STRING);
          }
          optional BYTE_ARRAY imei (STRING);
          optional BYTE_ARRAY instance_uid (STRING);
          optional BYTE_ARRAY interface_name (STRING);
          optional BYTE_ARRAY interface_uid (STRING);
          optional BYTE_ARRAY ip (STRING);
          optional BOOLEAN is_compliant;
          optional BOOLEAN is_managed;
          optional BOOLEAN is_personal;
          optional BOOLEAN is_trusted;
          optional INT64 last_seen_time (INTEGER(64, true));
          optional group location {
            optional BYTE_ARRAY city (STRING);
            optional BYTE_ARRAY continent (STRING);
            optional group coordinates (LIST) {
              repeated group list {
              }
            }
            optional BYTE_ARRAY country (STRING);
            optional BYTE_ARRAY desc (STRING);
            optional BYTE_ARRAY geohash (STRING);
            optional BOOLEAN is_on_premises;
            optional BYTE_ARRAY isp (STRING);
            optional DOUBLE lat;
            optional DOUBLE long;
            optional BYTE_ARRAY postal_code (STRING);
            optional BYTE_ARRAY provider (STRING);
            optional BYTE_ARRAY region (STRING);
          }
          optional BYTE_ARRAY mac (STRING);
          optional INT64 modified_time (INTEGER(64, true));
          optional BYTE_ARRAY name (STRING);
          optional group network_interfaces (LIST) {
            repeated group list {
              optional BYTE_ARRAY hostname (STRING);
              optional BYTE_ARRAY ip (STRING);
              optional BYTE_ARRAY mac (STRING);
              optional BYTE_ARRAY name (STRING);
              optional BYTE_ARRAY namespace (STRING);
              optional INT64 subnet_prefix (INTEGER(64, true));
              optional BYTE_ARRAY type (STRING);
              optional INT32 type_id (INTEGER(32, true));
              optional BYTE_ARRAY uid (STRING);
            }
          }
          optional group org {
            optional BYTE_ARRAY name (STRING);
            optional BYTE_ARRAY ou_name (STRING);
            optional BYTE_ARRAY ou_uid (STRING);
            optional BYTE_ARRAY uid (STRING);
          }
          optional group os {
            optional BYTE_ARRAY build (STRING);
            optional BYTE_ARRAY country (STRING);
            optional BYTE_ARRAY cpe_name (STRING);
            optional INT64 cpu_bits (INTEGER(64, true));
            optional BYTE_ARRAY edition (STRING);
            optional BYTE_ARRAY lang (STRING);
            optional BYTE_ARRAY name (STRING);
            optional BYTE_ARRAY sp_name (STRING);
            optional INT64 sp_ver (INTEGER(64, true));
            optional BYTE_ARRAY type (STRING);
            optional INT32 type_id (INTEGER(32, true));
            optional BYTE_ARRAY version (STRING);
          }
          optional group owner {
            optional group account {
              optional group labels (LIST) {
                repeated group list {
                }
              }
              optional BYTE_ARRAY name (STRING);
              optional BYTE_ARRAY type (STRING);
              optional INT32 type_id (INTEGER(32, true));
              optional BYTE_ARRAY uid (STRING);
            }
            optional BYTE_ARRAY credential_uid (STRING);
            optional BYTE_ARRAY domain (STRING);
            optional BYTE_ARRAY email_addr (STRING);
            optional BYTE_ARRAY full_name (STRING);
            optional group groups (LIST) {
              repeated group list {
                optional BYTE_ARRAY desc (STRING);
                optional BYTE_ARRAY domain (STRING);
                optional BYTE_ARRAY name (STRING);
                optional group privileges (LIST) {
                  repeated group list {
                  }
                }
                optional BYTE_ARRAY type (STRING);
                optional BYTE_ARRAY uid (STRING);
              }
            }
            optional BYTE_ARRAY ldap_person (JSON);
            optional BYTE_ARRAY name (STRING);
            optional group org {
              optional BYTE_ARRAY name (STRING);
              optional BYTE_ARRAY ou_name (STRING);
              optional BYTE_ARRAY ou_uid (STRING);
              optional BYTE_ARRAY uid (STRING);
            }
            optional BYTE_ARRAY risk_level (STRING);
            optional INT32 risk_level_id (INTEGER(32, true));
            optional INT64 risk_score (INTEGER(64, true));
            optional BYTE_ARRAY type (STRING);
            optional INT32 type_id (INTEGER(32, true));
            optional BYTE_ARRAY uid (STRING);
            optional BYTE_ARRAY uid_alt (STRING);
          }
          optional BYTE_ARRAY region (STRING);
          optional BYTE_ARRAY risk_level (STRING);
          optional INT32 risk_level_id (INTEGER(32, true));
          optional INT64 risk_score (INTEGER(64, true));
          optional BYTE_ARRAY subnet (STRING);
          optional BYTE_ARRAY subnet_uid (STRING);
          optional BYTE_ARRAY type (STRING);
          optional INT32 type_id (INTEGER(32, true));
          optional BYTE_ARRAY uid (STRING);
          optional BYTE_ARRAY uid_alt (STRING);
          optional BYTE_ARRAY vlan_uid (STRING);
          optional BYTE_ARRAY vpc_uid (STRING);
          optional BYTE_ARRAY zone (STRING);
        }
        optional BYTE_ARRAY log_level (STRING);
        optional BYTE_ARRAY log_name (STRING);
        optional BYTE_ARRAY log_provider (STRING);
        optional BYTE_ARRAY log_version (STRING);
        optional INT64 logged_time (INTEGER(64, true));
        optional BYTE_ARRAY name (STRING);
        optional group product {
          optional BYTE_ARRAY cpe_name (STRING);
          optional group feature {
            optional BYTE_ARRAY name (STRING);
            optional BYTE_ARRAY uid (STRING);
            optional BYTE_ARRAY version (STRING);
          }
          optional BYTE_ARRAY lang (STRING);
          optional BYTE_ARRAY name (STRING);
          optional BYTE_ARRAY path (STRING);
          optional BYTE_ARRAY uid (STRING);
          optional BYTE_ARRAY url_string (STRING);
          optional BYTE_ARRAY vendor_name (STRING);
          optional BYTE_ARRAY version (STRING);
        }
        optional INT64 transmit_time (INTEGER(64, true));
        optional BYTE_ARRAY uid (STRING);
        optional BYTE_ARRAY version (STRING);
      }
    }
    optional INT64 modified_time (INTEGER(64, true));
    optional BYTE_ARRAY original_time (STRING);
    optional INT64 processed_time (INTEGER(64, true));
    optional group product {
      optional BYTE_ARRAY cpe_name (STRING);
      optional group feature {
        optional BYTE_ARRAY name (STRING);
        optional BYTE_ARRAY uid (STRING);
        optional BYTE_ARRAY version (STRING);
      }
      optional BYTE_ARRAY lang (STRING);
      optional BYTE_ARRAY name (STRING);
      optional BYTE_ARRAY path (STRING);
      optional BYTE_ARRAY uid (STRING);
      optional BYTE_ARRAY url_string (STRING);
      optional BYTE_ARRAY vendor_name (STRING);
      optional BYTE_ARRAY version (STRING);
    }
    optional group profiles (LIST) {
      repeated group list {
      }
    }
    optional INT64 sequence (INTEGER(64, true));
    optional BYTE_ARRAY tenant_uid (STRING);
    optional BYTE_ARRAY uid (STRING);
    optional BYTE_ARRAY version (STRING);
  }
  optional group observables (LIST) {
    repeated group list {
      optional BYTE_ARRAY name (STRING);
      optional group reputation {
        optional DOUBLE base_score;
        optional BYTE_ARRAY provider (STRING);
        optional BYTE_ARRAY score (STRING);
        optional INT32 score_id (INTEGER(32, true));
      }
      optional BYTE_ARRAY type (STRING);
      optional INT32 type_id (INTEGER(32, true));
      optional BYTE_ARRAY value (STRING);
    }
  }
  optional BYTE_ARRAY prev_security_level (STRING);
  optional INT32 prev_security_level_id (INTEGER(32, true));
  optional group prev_security_states (LIST) {
    repeated group list {
      optional BYTE_ARRAY state (STRING);
      optional INT32 state_id (INTEGER(32, true));
    }
  }
  optional BYTE_ARRAY raw_data (STRING);
  optional BYTE_ARRAY security_level (STRING);
  optional INT32 security_level_id (INTEGER(32, true));
  optional group security_states (LIST) {
    repeated group list {
      optional BYTE_ARRAY state (STRING);
      optional INT32 state_id (INTEGER(32, true));
    }
  }
  optional BYTE_ARRAY severity (STRING);
  optional INT32 severity_id (INTEGER(32, true));
  optional INT64 start_time (INTEGER(64, true));
  optional BYTE_ARRAY state (STRING);
  optional INT32 state_id (INTEGER(32, true));
  optional BYTE_ARRAY status (STRING);
  optional BYTE_ARRAY status_code (STRING);
  optional BYTE_ARRAY status_detail (STRING);
  optional INT32 status_id (INTEGER(32, true));
  optional INT64 time (INTEGER(64, true));
  optional INT64 timezone_offset (INTEGER(64, true));
  optional BYTE_ARRAY type_name (STRING);
  optional INT64 type_uid (INTEGER(64, true));
  optional BYTE_ARRAY unmapped (JSON);
}
